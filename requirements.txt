# 智能视频水印去除工具 - 生产环境依赖
# 仅包含运行应用所必需的依赖

# ===== 核心GUI框架 =====
PyQt6>=6.6.0

# ===== 计算机视觉和图像处理 =====
opencv-python>=4.8.0
numpy>=1.25.0
Pillow>=10.0.0
psutil>=5.9.0

# ===== 深度学习框架 (Phase 5-6 必需) =====
# Phase 5: GPU 加速 inpainting (U-Net)
# Phase 6: YOLO 水印检测
torch>=2.6.0
torchvision>=0.21.0
torchaudio>=2.6.0

# ===== YOLO 水印检测 (Phase 6) =====
ultralytics>=8.3.0

# ===== ONNX运行时 (可选，用于模型推理) =====
# onnx>=1.15.0
# onnxruntime>=1.16.0  # CPU版本
# onnxruntime-gpu>=1.16.0  # GPU版本，需要CUDA支持

# ===== 系统依赖说明 =====
# 1. FFmpeg: 需要系统级安装（用于音频处理）
#    - Windows: 从 https://ffmpeg.org/download.html 下载并添加到PATH
#    - Ubuntu/Debian: sudo apt install ffmpeg
#    - macOS: brew install ffmpeg
#
# 2. 深度学习框架安装（可选）:
#    GPU版本 (需要NVIDIA GPU和CUDA):
#      pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#      pip install onnxruntime-gpu
#
#    CPU版本 (推荐用于大多数用户):
#      pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
#      pip install onnxruntime
#
# 3. 安装命令:
#    pip install -r requirements.txt
